services:
  gameoflife.server:
    build:
      context: GameOfLife.Server
      dockerfile: Dockerfile
    ports: 
    - "5081:8080"
  
  gameoflife.client:
    build:
      context: GameOfLife.Client
      dockerfile: Dockerfile
    ports: 
    - "4200:4200"
